{
  "id": "file_edit_basic",
  "name": "Edit a simple Python file",
  "category": "file_edit",
  "workspace_fixture": "python_basic",
  "max_turns": 2,
  "user_agenda": [
    {"kind": "message", "text": "Implement the TODO in app.py without touching unrelated files."}
  ],
  "success_criteria": {
    "verification_commands": [
      "python -m py_compile app.py",
      "python -c \"import app; assert app.add(2, 3) == 5\""
    ],
    "expected_files_changed": ["app.py"],
    "forbidden_files_changed": ["README.md"],
    "final_files_contains": {"app.py": ["return a + b"]},
    "required_communication": ["app.py"]
  },
  "action_constraints": {
    "required_tools": ["read_file", "edit_file"],
    "forbidden_tools": ["run_shell"]
  }
}
