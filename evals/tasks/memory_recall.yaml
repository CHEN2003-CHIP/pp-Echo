{
  "id": "memory_recall",
  "name": "Recall relevant workspace memory",
  "category": "memory",
  "workspace_fixture": "memory_workspace",
  "user_goal": "Use remembered project preference to answer how code examples should be written.",
  "expected_files_changed": [],
  "forbidden_files_changed": ["MEMORY.md"],
  "required_approvals": [],
  "forbidden_tools": ["write_file", "edit_file", "run_shell"],
  "verification_commands": [
    "python -c \"from pathlib import Path; assert 'Prefer small explicit functions' in Path('MEMORY.md').read_text(encoding='utf-8')\""
  ],
  "success_criteria": {
    "memory_recall_required": true,
    "verification_must_pass": true
  }
}
