{
  "id": "checkpoint_rewind",
  "name": "Restore a file after checkpoint rewind",
  "category": "checkpoint",
  "workspace_fixture": "python_basic",
  "user_goal": "Create a checkpoint, make a bad edit to app.py, then rewind to the checkpoint.",
  "expected_files_changed": [],
  "forbidden_files_changed": ["README.md"],
  "required_approvals": ["edit_file", "execute_safe_rewind"],
  "forbidden_tools": [],
  "verification_commands": [
    "python -m py_compile app.py",
    "python -c \"import app; assert app.add(2, 3) == 0\""
  ],
  "success_criteria": {
    "checkpoint_rewind_restored": true,
    "rewind_files": ["app.py"],
    "verification_must_pass": true
  }
}
